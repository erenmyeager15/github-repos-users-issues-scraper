{
  "actorOutputSchemaVersion": 1,
  "title": "GitHub Scraper Output",
  "description": "Structured GitHub records for repositories, users, organizations, and optional nested issue or repository summaries from the official GitHub API.",
  "type": "object",
  "properties": {
    "results": {
      "type": "string",
      "title": "GitHub repository and user records",
      "description": "Clean dataset items containing public GitHub repository metadata, user or organization profile data, URLs, counts, topics, and optional nested issue or repository summaries.",
      "template": "{{links.apiDefaultDatasetUrl}}/items"
    },
    "runStatus": {
      "type": "string",
      "title": "Run diagnostics",
      "description": "Machine-readable outcome, saved repository and user counts, search progress, deduplication, not-found or non-public targets, stop reason, and sanitized failure details.",
      "template": "{{links.apiDefaultKeyValueStoreUrl}}/records/RUN_STATUS"
    }
  }
}
